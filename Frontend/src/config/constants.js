export const APP_NAME = import.meta.env.VITE_APP_NAME || 'Hackathon Project';
export const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:5000/api';

export const ROUTES = {
  HOME: '/',
  LOGIN: '/login',
  REGISTER: '/register',
  DASHBOARD: '/dashboard',
};

export const API_ENDPOINTS = {
  HEALTH: '/health',
  // Add more endpoints as needed
};
